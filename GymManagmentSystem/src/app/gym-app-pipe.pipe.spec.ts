import { GymAppPipePipe } from './gym-app-pipe.pipe';

describe('GymAppPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GymAppPipePipe();
    expect(pipe).toBeTruthy();
  });
});
